# Peer deps resueltos automaticamente: el ecosistema Nest declara peers laxos.
auto-install-peers=true
strict-peer-dependencies=false

# Solo estos paquetes pueden ejecutar scripts de post-instalacion.
# Evita ejecucion arbitraria de codigo desde el arbol de dependencias.
enable-pre-post-scripts=false
